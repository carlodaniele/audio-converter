# Screenshot Workspace Notes

This folder is intentionally kept minimal while preparing real product screenshots.

Use docs/wporg-screenshot-capture-runbook-en.md as the source of truth for
capture order, content, and final verification.

Note: for WordPress.org listing assets, use the required filenames and dimensions in
the SVN top-level assets folder after slug approval.
